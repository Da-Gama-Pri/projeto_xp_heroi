let nome_heroi = ("Genoveva")
let XP = 900

switch (XP) {
    case XP < 1000:
        console.log ("Nivel Ferro")
        break;

    default:
        break;
} (XP)

console.log ("Seu jogador " + nome_heroi + " é nivel " + XP)